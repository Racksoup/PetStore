import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const About = (props) => {
  return (
    <Fragment>
      <div> about</div>;
    </Fragment>
  );
};

About.propTypes = {};

export default About;
